dist: trusty
language: cpp

cache:
  ccache: true
  directories:
    - dependencies

env:
  global:
    - MAKEFLAGS="-j 2"

branches:
  except:
    - gh-pages

script:
  - wget https://dl.bintray.com/helics/develop/HELICS_2_0/helics-install-$(uname -s).sh
  - ./helics-install-Linux.sh --skip-license --prefix=helics
  - helics/bin/helics_broker --version
  - mkdir build && cd build
  - cmake .. -DAUTOBUILD_HELICS=ON -DBUILD_RELEASE_ONLY=ON
  - make ${MAKEFLAGS}

notifications:
    email: false
